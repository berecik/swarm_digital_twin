# values-cloud.yaml — EKS / GKE / AKS overrides
# Usage: helm install swarm ./helm/swarm-digital-twin -f ./helm/swarm-digital-twin/values-cloud.yaml

drones: 6

images:
  sitl:
    repository: beret/ardupilot-sitl
    tag: latest
    pullPolicy: Always
  companion:
    repository: beret/swarm_companion
    tag: latest
    pullPolicy: Always

service:
  mavlink:
    type: ClusterIP
