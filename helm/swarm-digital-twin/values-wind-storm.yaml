# Wind profile: storm-like (Dryden, base 12 m/s + 0.6 intensity).
# Mirrors the `[storm]` entry in `gazebo/worlds/wind/manifest.toml`.
# Phase 5c: data-only stub. Storm runs are expected to abort some
# missions — the stress envelope only requires > 50% completion.

wind:
  profile: dryden
  base_speed_ms: 12.0
  direction: [1.0, 0.0, 0.0]
  gust_intensity: 0.6
  seed: 42
