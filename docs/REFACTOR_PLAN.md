# Roadmap

**Reference paper:** Valencia et al., *An Open-source UAV Digital Twin framework*, J. Intell. & Robot. Syst. 111:71 (2025), DOI: `10.1007/s10846-025-02276-7`

**Scope:** remaining gaps and future work only. Completed items are in [`CHANGELOG.md`](../CHANGELOG.md).

**Current state (2026-04-18):** 299 tests passing. All paper-aligned items delivered. Live Run-time View is the default visualization. Physics Live Replay streams simulations to the browser without Docker.

---

## Delivered (see CHANGELOG.md for details)

- Paper equations 1–7, tables 1–5 — fully implemented and verified
- Run-time View: FastAPI + Three.js live viewer with WebSocket telemetry
- Physics Live Replay: `--physics-live`, `--physics-swarm-live` modes
- Waypoint markers in the live 3D view
- Flask → FastAPI migration
- Default `./run_scenario.sh` runs physics sim + live viewer (looping)
- 282 tests (242 physics + 40 RTV/live-replay)

---

## Backlog — upcoming features

### ~~1) Multi-drone live view~~ ✅ Done

### ~~2) Browser-driven command execution~~ ✅ Done

### ~~3) Post-flight replay in the web viewer~~ ✅ Done

### ~~4) DataFlash-compatible `.BIN` recording~~ ✅ Done

### ~~5) Windows browser auto-open~~ ✅ Done

### 6) Real mission thumbnails

Placeholder PNGs generated by Pillow. Replace with screenshots from
real SITL runs.

### 7) Authentication / multi-user

The server binds to `127.0.0.1` only. LAN exposure would require auth
and CSRF protection.

### ~~8) websockets deprecation warnings~~ ✅ Done

---

## Release checklist (`life_vision` → `master`)

### Automated gate

| Check | Command | Status |
|:---|:---|:---|
| Full test suite (299) | `pytest simulation/test_drone_physics.py -q` | ✅ |
| JS syntax | `node --check simulation/runtime_view/web/live.js` | ✅ |
| Server import | `python -c "from simulation.runtime_view.server import app"` | ✅ |
| Telemetry import | `cd simulation && python -c "from live_telemetry import MAVLinkLiveSource"` | ✅ |

### Manual smoke tests

- [ ] `./run_scenario.sh` — physics sim streams to live viewer (looping),
      drone flies waypoint pattern, waypoint markers visible, HUD updates.
- [ ] `./run_scenario.sh --viz-live` — bare server, status shows DISCONNECTED.
- [ ] `./run_scenario.sh --physics-live` — single-shot physics replay.
- [ ] `./run_scenario.sh --physics-swarm-live` — swarm replay (first drone).
- [ ] `./run_scenario.sh --single-live` — SITL + live viewer (requires Docker).
- [ ] `./run_scenario.sh --single-static` — matplotlib post-flight replay.
- [ ] Ctrl-C shuts down server + receiver within 2 s.
- [ ] WebSocket reconnect: kill/restart server → status flips back to CONNECTED.
- [ ] Chrome + Firefox render `/live` correctly.

### Merge plan

1. Complete manual smoke tests.
2. Fix any blockers.
3. Merge `life_vision` → `master`.
4. Update `MAINTENANCE.log` with final test count.
